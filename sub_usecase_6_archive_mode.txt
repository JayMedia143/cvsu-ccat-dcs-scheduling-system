%%{init: {"flowchart": {"nodeSpacing": 25, "rankSpacing": 50}}}%%
flowchart LR
    %% Actors
    SA["👤 Superadmin"]
    DA["👤 Admin"]

    SA -. inherits .-> DA

    %% Subsystem Boundary (Strict UI Terminology)
    subgraph Subsystem ["Term Archive & History Mode"]
        CAPTURE([Archive Current Schedule])
        VIEW([View Archived Schedule])
        LOCK([Set Read-Only Archive])
        RESTORE([Restore Schedule Archive])
        SEARCH([Search Archive History])
    end

    %% Connections
    DA --> CAPTURE
    DA --> VIEW
    VIEW --> LOCK
    VIEW --> RESTORE
    VIEW --> SEARCH

    %% Styling
    classDef actor fill:#f3f4f6,stroke:#4b5563,stroke-width:2px,color:#1f2937,font-weight:bold,font-size:18px;
    classDef usecase fill:#e0f2fe,stroke:#0284c7,stroke-width:2px,color:#0369a1,font-weight:bold,font-size:18px;
    classDef sys fill:#ffffff,stroke:#1e3a8a,stroke-width:3px,color:#1e3a8a,font-weight:bold,font-size:20px;

    class SA,DA actor;
    class CAPTURE,VIEW,LOCK,RESTORE,SEARCH usecase;
    class Subsystem sys;
