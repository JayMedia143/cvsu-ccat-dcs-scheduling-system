%%{init: {"flowchart": {"nodeSpacing": 25, "rankSpacing": 50}}}%%
flowchart LR
    %% Actors
    DA["👤 Admin"]
    UF["👤 User"]

    DA -. inherits .-> UF

    %% Subsystem Boundary
    subgraph Subsystem ["Draft Workflow & Proposal Hub"]
        DRAFT([Create Schedule Draft])
        PROPOSE([Submit Schedule Proposal])
        ATTACH([Attach Schedule Draft])
        CHAT([Live Hub Chat])
        EVALUATE([Evaluate Schedule Proposal])
        MERGE([Merge Schedule to Master])
    end

    %% Connections
    UF --> DRAFT
    UF --> PROPOSE
    UF --> ATTACH
    UF --> CHAT

    DA --> EVALUATE
    DA --> MERGE

    %% Styling
    classDef actor fill:#f3f4f6,stroke:#4b5563,stroke-width:2px,color:#1f2937,font-weight:bold,font-size:18px;
    classDef usecase fill:#e0f2fe,stroke:#0284c7,stroke-width:2px,color:#0369a1,font-weight:bold,font-size:18px;
    classDef sys fill:#ffffff,stroke:#1e3a8a,stroke-width:3px,color:#1e3a8a,font-weight:bold,font-size:20px;

    class DA,UF actor;
    class DRAFT,PROPOSE,ATTACH,CHAT,EVALUATE,MERGE usecase;
    class Subsystem sys;
